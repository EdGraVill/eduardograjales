/// <reference types="react-scripts" />

interface PostPreview {
  title: string;
  id: string;
  description: string;
  date: Date;
}

interface Post {
  title: string;
  id: string;
  content: string;
  date: Date;
}
